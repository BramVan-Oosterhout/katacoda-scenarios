FROM bramvanoosterhout/foswiki1
COPY answer.pl /tmp/answer
WORKDIR /var/www/foswiki/bin
RUN ./manage -action=createweb -baseweb=_default -newweb=Sandbox/Exercises -webbgcolor=green -websummary="Web for exercises" \
    chown -R daemon:daemon /var/www/*
    
